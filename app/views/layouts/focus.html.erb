<!DOCTYPE html>
<html>
  <head>
    <title>TheFavorite</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,200..300,0..1,0" />
  </head>

  <body class="d-flex flex-column" style="height: 100vh">
    <div class="flex-grow-1 overflow-scroll site-content position-relative">
      <%= render "shared/flashes_absolute" %>
      <%= link_to (session[:origin_path] ? session[:origin_path] : :back) || root_path, class: "container d-flex align-items-center fs-5 p-0" do %>
        <i class="ri-arrow-left-line fs-1"></i>
      <% end %>
      <%= yield %>
    </div>
    <%= render "shared/navbar" %>
  </body>
</html>
